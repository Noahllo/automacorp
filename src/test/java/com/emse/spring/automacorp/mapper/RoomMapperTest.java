package com.emse.spring.automacorp.mapper;

import org.junit.jupiter.api.Test;

class RoomMapperTest {

    @Test
    void shouldMapRoom() {
        // Arrange

        // Act
        /*
        RoomDto room = RoomMapper.of(RoomEntity);

        // Assert
        RoomDto expectedRoom = new RoomDto(
                11L,
                "Room1Building",
                1,
                23.2,
                26.4,
                Set.of(new WindowDto(111L, "Window1Room1Building", 11L),
                        new WindowDto(112L, "Window2Room1Building", 11L))
        );
        Assertions.assertThat(room).usingRecursiveAssertion().isEqualTo(expectedRoom);

         */
    }
}